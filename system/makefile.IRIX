#!/bin/make

include ../libpaths.IRIX

INCDIRS = -I. \
	  -I../math \
	  -I../scene \
	  -I../input \
	  -I../motion \
	  -I/usr/include/X11 \
          $(EXTRA_INCS)

LIBNAME = libvsSystem.a

TYPE = -n32 -mips3
FLAGS = $(TYPE) -g -xansi -DSGI -DPF_CPLUSPLUS_API=1 -woff 1681 $(INCDIRS)
LINKFLAGS = $(TYPE) -ignore_unresolved -Wl,-woff,85
ARFLAGS = -ruc


all: $(LIBNAME)


SRCS = vsDatabaseLoader.c++ \
       vsGraphicsState.c++ \
       vsGrowableArray.c++ \
       vsObject.c++ \
       vsObjectMap.c++ \
       vsPane.c++ \
       vsPipe.c++ \
       vsScreen.c++ \
       vsSystem.c++ \
       vsTreeMap.c++ \
       vsUpdatable.c++ \
       vsView.c++ \
       vsWindow.c++

OBJS = vsDatabaseLoader.o \
       vsGraphicsState.o \
       vsGrowableArray.o \
       vsObject.o \
       vsObjectMap.o \
       vsPane.o \
       vsPipe.o \
       vsScreen.o \
       vsSystem.o \
       vsTreeMap.o \
       vsUpdatable.o \
       vsView.o \
       vsWindow.o

$(LIBNAME): $(OBJS)
	ar -rc $(LIBNAME) $(OBJS)


dep: depend
depend:
	makedepend -f makefile.IRIX -- $(FLAGS) -- $(SRCS)


clean:
	rm -f *.o $(LIBNAME) core makefile.*.bak


.c++.o:
	CC $(FLAGS) -c $< -o ./$*.o

.c.o:
	CC $(FLAGS) -c $< -o ./$*.o

